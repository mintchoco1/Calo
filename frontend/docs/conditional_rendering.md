React의 핵심
지금은 meals 배열에 항상 3개의 식사가 있음. 근데 진짜 앱에서는
새 사용자가 처음 켰을 때->기록 없음
새로운 하루가 시작됐을 때->아직 안 먹음
사용자가 모든 기록을 지웠을 때->비어 있음
이럴 때 빈 칸으로 두면 어색함
빈 상태일 때 -> 안내 메시지 띄우기

조건부 렌더링
JSX 안에서 "~한 경우에 A를, 아니면 B를 보여줘" 하는 패턴
자바스크립트에서는 두 가지 방법 사용
방법 1: 삼항 연산자 ? :
조건 ? 참일때값 : 거짓일때값
방법 2:단축 평가 &&
조건 && 참일때값